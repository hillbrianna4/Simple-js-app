let pokemonList = [];
pokemonList = [{ name: "bulbasaur", height:"0.7m" ,[type: "grass"]}
,{name: "Marrill", height:"0.4m" ,[type:"water"]},
{name:"gothorita" ,height:"0.7m" ,[type:"Psychic"]}];
for(let = 0; i<pokemonList.length i++){
  if pokemonList[i.height<0.8 &&pokemonList[i].height>0.4]}
  document.write(pokemonList[i].name + is " is a big one!");
  else if (pokemonList[i].height<0.4){
    document.write(pokemonList[i].name + is +" is a small one!");
  }
}
